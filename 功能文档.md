# 智能日常助手 APP —— 功能设计文档

---

## 基本信息

- **App名称**：随记
- **功能模块**：时间事务管理模块 + 情绪生活陪伴模块 + 全局支撑模块
- **技术栈**：Flutter 3.x + Dart 3.6 + Provider 6.1 + Hive 2.2 + HTTP 1.2 + SharedPreferences 2.3 + flutter_local_notifications 18.0 + Material Design 3 + DeepSeek API
- **目标用户**：在校大学生（存在目标规划混乱、任务拖延、时间利用率低、情绪内耗等痛点）

---

## 功能描述

智能日常助手是一款面向大学生的个人成长管家兼情感陪伴工具。App 围绕两大核心场景设计：

1. **时间事务管理**：学生面临多门课程、考级备考、日常琐事的并行压力，常因缺乏系统规划导致任务堆积。本模块提供目标设定、待办管理、日历视图、番茄钟专注计时和统计数据看板，帮助用户实现「目标拆解 → 任务执行 → 数据反馈」的闭环。

2. **情绪生活陪伴**：学业压力带来的焦虑、孤独等情绪是大学生群体的常见问题。本模块提供一个仅自己可见的私密生活圈（图文日记），以及基于 DeepSeek 的 AI 陪伴对话能力，让用户在倾诉中获得共情回应和情绪支持。

两个模块通过数据联动形成完整闭环：待办 → 番茄钟 → 专注记录 → 统计看板；动态内容 → AI 情绪识别 → 陪伴回应。

---

## 核心需求

1. 用户能够创建和管理长期/短期目标，通过子目标拆解和进度百分比直观追踪备考/学习计划的完成情况。
2. 用户能够新增、编辑、标记完成待办事项，并按日期在月历视图中查看每日任务分布，封面优先级颜色快速辨识当天重点。
3. 用户能够启动番茄钟专注计时（默认 25 分钟或自定义时长），可选择绑定待办任务，计时完成后自动记录专注时长。
4. 用户能够在首页看板中查看当日待办完成进度、目标概览和番茄钟快捷入口，在"我的"页面查看专注次数、待办完成数和近 7 天专注趋势等统计数据。
5. 用户能够发布带有心情标签和可选图片的生活动态，系统自动调用 AI 生成共情评论，用户可在动态下方与 AI 进行多轮对话追问。
6. 用户能够在独立对话界面与 AI 小助手进行自由多轮文字对话，AI 基于系统人设（温暖、简短、积极）进行回复。
7. 用户能够编辑个人昵称、账号、签名和头像，切换深色模式，管理通知偏好设置。
8. 所有用户数据（待办、目标、番茄钟记录、动态、对话）持久化存储在本地，退出 App 后数据不丢失。

---

## 功能概述

App 包含 **13 个页面**、**4 个底部导航 Tab**，分为三大功能域：

| 功能域 | 核心页面 | 所属 Tab |
|--------|---------|----------|
| 今日看板 | 首页（HomePage）、番茄钟页（PomodoroPage） | 首页 Tab |
| 事务管理 | 日历页（CalendarPage）、待办表单（TodoFormPage）、待办详情（TodoDetailPage）、目标表单（GoalFormPage）、目标详情（GoalDetailPage） | 日历 Tab |
| 生活陪伴 | 生活页（LifePage）、发布动态（PostFormPage）、动态详情（PostDetailPage） | 生活 Tab |
| 个人中心 | 我的（ProfilePage）、个人信息编辑（ProfileEditPage）、通知设置（NotificationSettingsPage）、使用统计（UsageStatsPage） | 我的 Tab |

**跨模块数据联动**：
- 待办 → 番茄钟：番茄钟页面可从当日待办列表中绑定任务，计时结束时自动标记该待办为已完成。
- 番茄钟 → 日历：日历日详情中展示当日番茄钟累计专注时长，数据来源于番茄钟记录。
- 番茄钟 → 统计：使用统计页的环形成就图和柱状趋势图展示番茄钟历史数据。
- 待办 → 统计：使用统计页展示本周待办完成率；"我的"页面展示今日已完成的待办数量。
- 动态 → AI 对话：发布动态后 AI 自动生成首条共情评论，用户可在动态下方继续与 AI 对话。

---

## 业务流程

### 流程一：目标管理

```
用户打开 App → 首页可见目标卡片网格
  → 点击 [+ 新增] → GoalFormPage（输入目标标题、描述、截止日期）
  → 提交 → GoalProvider.addGoal() → Hive 持久化 → 返回首页，目标卡片更新

用户点击目标卡片 → GoalDetailPage
  → 查看目标详情（进度条、剩余天数、子目标列表）
  → 点击顶部按钮 → 切换目标状态（进行中 ↔ 已完成）
  → 底部输入子目标标题 + 选择截止日期 → 点击 [+ 添加]
  → 点击子目标 → 切换完成/未完成状态 → 主目标进度自动重新计算
```

### 流程二：待办管理 + 日历联动

```
日历月视图中点击某日期 → 下方展开当日详情（待办列表 + 完成率 + 专注时长）
  → 待办列表项左侧显示优先级颜色条（红 = 高, 黄 = 中, 绿 = 低）
  → 点击待办项 → TodoDetailPage（查看/编辑详情）
  → 点击 [+ 新增待办] → TodoFormPage
      → 填写标题（必填）、描述、优先级、截止时间
      → 提交 → TodoProvider.addTodo() → Hive 持久化 → 返回日历页刷新

首页今日待办区域 → 与日历共享同一数据源
  → 点击待办项左侧按钮 → 切换完成/未完成 → 日历视图同步更新
```

### 流程三：番茄钟专注

```
入口一：首页番茄钟卡片 → 点击 [开始]（默认 25 分钟）或 [自定义]（底部弹窗输入分钟数）
入口二：点击首页番茄钟卡片主体 → PomodoroPage（完整功能）

PomodoroPage：
  → 顶部下拉菜单（可选）→ 从待办列表中选择绑定的待办任务
  → 点击 [▶ 开始] → 圆形倒计时开始
  → 运行中操作：
      [⏸ 暂停] → 暂停计时
      [🔄 重置] → 恢复为初始时长
      [⏹ 结束] → 停止计时，PomodoroProvider.addRecord() 记录本次时长
  → 倒计时归零自动完成 → 记录专注时长 → 若绑定了待办则自动标记该待办完成
```

### 流程四：生活动态发布 + AI 对话

```
生活页 [动态] Tab → 时间线展示历史动态（按时间倒序，今天/昨天/更早分组）
  → 每条动态显示：心情 emoji + 文字内容 + 发布时间 + AI 评论（如有图片则展示图片）

点击 FAB [+] → PostFormPage：
  → 选择心情（😊开心 / 😐平静 / 😰焦虑 / 😫疲惫 / 😢难过）
  → 输入文字内容（maxLines: 8）
  → 可选：点击图片区域 → 底部弹窗选择 [拍照] 或 [从相册选择]
      → 选择后自动裁剪为 256×256 px 正方形 → 预览框展示
  → 点击 [发布] 按钮：
      1. Post 对象写入 Hive → Navigator.pop → 动态时间线刷新
      2. 异步调用 DeepSeekService.chatText() 基于文字 + 心情生成 AI 评论
      3. AI 返回后更新 post.replies[0] → Hive 回写 → UI 刷新展示 AI 评论

动态详情 PostDetailPage：
  → 展开完整内容 + 图片
  → 对话线程区域：AI 首评 + 用户追问 + AI 多轮回复
  → 底部输入框 → 用户输入追问 → LifeProvider.replyToPost() → AI 回复
```

### 流程五：AI 自由对话

```
生活页 [AI小助手] Tab → 多轮对话界面
  → 启动时自动加载历史对话 → 对话气泡展示（用户消息右侧绿色，AI 消息左侧蓝色）
  → 底部输入框 → 输入文字 → [发送]
  → LifeProvider.sendToAI() → 取最近 40 条消息构建 history
  → DeepSeekService.chatConversation() → AI 回复 → 会话保存 Hive → 对话刷新
```

### 流程六：个人中心与统计

```
我的 Tab → ProfilePage：
  → 顶部：头像 + 昵称 + 签名（点击跳转 ProfileEditPage）
  → 统计数据卡片：专注次数（PomodoroProvider）、待办完成数（TodoProvider）
  → 设置列表：
      个人信息设置 → ProfileEditPage（修改后保存到 Hive + UserProvider 通知）
      深色模式 → Switch 实时切换
      通知设置 → NotificationSettingsPage（各通知类型独立开关）
      使用统计 → UsageStatsPage（本周完成率、今日/本周专注时长、近 7 天趋势柱状图）
      关于应用 → 版本信息展示
```

---

## 详细功能说明

### 模块一：时间与事务管理

#### 1.1 目标与备考规划

**数据模型**：[Goal](file:///d:/HarmonyOS/FlutterApplication/flutter_harmonyos/lib/models/goal.dart) / SubGoal

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| title | String | 是 | 目标名称，如"考研上岸" |
| description | String | 否 | 目标描述，如"目标院校：华南理工大学" |
| deadline | DateTime | 是 | 截止日期，用户选择 |
| progress | double | 否 | 0.0~1.0，由子目标完成比例自动计算 |
| status | String | 否 | "进行中" / "已完成"，可手动切换 |
| subGoals | List\<SubGoal\> | 否 | 子目标列表，每个含标题、截止日期、完成状态 |

**业务规则**：
- 目标 status 默认为"进行中"，用户可手动切换为"已完成"。
- 子目标完成时自动标记 `isCompleted = true`，主目标 progress 重新计算：`progress = 已完成子目标数 / 子目标总数`。
- 删除目标时其所有子目标同时删除。
- 目标 deadline 到达前 3 天，系统发送本地通知提醒（需 goal_reminder 开关启用）。
- 首页目标卡片展示 progress 百分比和剩余天数，剩余天数 = deadline − 今天，小于 0 时显示 0。

#### 1.2 待办与日程管理

**数据模型**：[Todo](file:///d:/HarmonyOS/FlutterApplication/flutter_harmonyos/lib/models/todo.dart)

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| title | String | 是 | 待办标题 |
| description | String | 否 | 详细描述 |
| priority | int | 是 | 3=高(红), 2=中(黄), 1=低(绿) |
| done | bool | 否 | 是否已完成，默认 false |
| date | DateTime | 是 | 关联日期，用于日历按日筛选 |
| time | TimeOfDay? | 否 | 提醒时间 |

**业务规则**：
- 新建待办时 title 不能为空。
- 待办可关联到任意日期（默认当天）。
- 日历月视图中，某日有待办则显示圆点标记，圆点颜色按当日最高优先级待办确定（存在高优先级 → 红色；存在中优先级 → 黄色；仅有低优先级 → 绿色）。
- 点击待办可切换完成/未完成状态。
- 待办删除不可恢复。

#### 1.3 日历视图

**交互规则**：
- 展示月历网格，首行为星期标题（日/一/二/三/四/五/六）。
- 左右滑动切换月份，跨年自动处理。
- 点击某日期 → 下方展开当日详情面板（再次点击收起）。
- 当日详情面板展示：
  - 当日待办列表（按优先级降序排列）
  - 当日待办完成率：已完成数 / 总数
  - 当日番茄钟专注总时长（从 PomodoroProvider 聚合）
  - [新增待办] 按钮

#### 1.4 番茄钟计时

**数据模型**：[PomodoroRecord](file:///d:/HarmonyOS/FlutterApplication/flutter_harmonyos/lib/models/pomodoro_record.dart)

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| date | DateTime | 是 | 记录日期 |
| durationMinutes | int | 是 | 专注时长（分钟） |
| todoId | String? | 否 | 绑定的待办 ID |
| todoTitle | String? | 否 | 绑定的待办标题 |

**业务规则**：
- 默认专注时长 25 分钟，首页可自定义（输入分钟数，范围 1~120）。
- 绑定待办时下拉菜单展示当日所有未完成的待办。
- 开始后圆形进度条从初始时长倒计时至 0。
- 暂停状态下可继续或重置。
- 计时主动结束或倒计时归零 → 写入 PomodoroRecord → 若绑定待办则标记该待办为已完成。
- 记录中包含绑定的 todoId 和 todoTitle（冗余存储，避免统计查询时二次查 Todo）。

#### 1.5 数据统计

**统计指标**：
| 指标 | 数据来源 | 展示位置 |
|------|---------|---------|
| 本周待办完成率 | TodoProvider | 使用统计页 |
| 今日专注时长 | PomodoroProvider | 我的页统计卡片 |
| 总专注次数 | PomodoroProvider | 我的页统计卡片 |
| 近 7 天专注趋势 | PomodoroProvider | 使用统计页柱状图 |
| 今日已完成待办数 | TodoProvider | 我的页今日成就 |

#### 1.6 智能小结

**触发条件**：用户手动触发或每日定时触发。

**生成规则**：
- 调用 `DeepSeekService.generateDailySummary(completedCount, totalCount, focusMinutes, nickname)`。
- 传入当日数据：已完成待办数、总待办数、专注总分钟数、用户昵称。
- AI 返回 50 字以内的温暖鼓励语。
- API 调用失败时使用预设降级文案："今天辛苦了，好好休息，明天继续加油 ✨"。

---

### 模块二：情绪与生活陪伴

#### 2.1 私密生活圈

**数据模型**：[Post](file:///d:/HarmonyOS/FlutterApplication/flutter_harmonyos/lib/models/post.dart)

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| content | String | 否 | 文字内容（与图片至少一个非空） |
| moodEmoji | String | 是 | 😊😐😰😫😢 之一 |
| moodLabel | String | 是 | 开心/平静/焦虑/疲惫/难过 |
| imageBase64 | String? | 否 | 图片 Base64 编码（256×256 裁剪后约 10~20KB） |
| replies | List\<ChatMessage\> | 否 | AI 对话线程 |
| createdAt | DateTime | 是 | 发布时间 |

**业务规则**：
- 发布动态时，content 和 imageBase64 至少一个不为空。
- 心情 emoji 和 label 必须选择其一。
- 发布后动态卡片立即显示在时间线顶部，如果 AI 评论尚未返回，该区域留空。
- AI 评论异步返回后回写 Hive 并刷新 UI。
- 每条动态右上角有删除按钮（× 图标），点击后弹出确认弹窗"确定删除这条动态？"，确认后删除且不可恢复。
- 动态按发布时间倒序排列，按日期分组（今天 / 昨天 / MM-dd）。
- 图片仅用于展示，不参与 AI 回复内容生成。

#### 2.2 AI 陪伴对话

**数据模型**：[ChatSession](file:///d:/HarmonyOS/FlutterApplication/flutter_harmonyos/lib/models/chat_session.dart) / [ChatMessage](file:///d:/HarmonyOS/FlutterApplication/flutter_harmonyos/lib/models/chat_session.dart)

| 字段（ChatMessage） | 类型 | 说明 |
|---------------------|------|------|
| role | String | 'user' 或 'ai' |
| content | String | 消息内容 |
| imagePath | String? | 预留字段（当前版本不使用） |
| timestamp | DateTime | 消息时间戳 |

| 字段（ChatSession） | 类型 | 说明 |
|---------------------|------|------|
| id | String | 会话 ID |
| title | String | 会话标题 |
| messages | List\<ChatMessage\> | 对话消息列表 |

**业务规则**：
- 仅支持文字对话，不支持图片输入。
- AI 回复长度限制：每条不超过 30 字（通过 systemPrompt 约束）。
- 对话历史取最近 40 条（约 20 轮）构建 context，超出部分截断。
- API 调用超时时间 15 秒；超时或网络异常时返回降级文案。

**情绪识别规则**（本地规则引擎，不调用 API）：
| 关键词 | 识别结果 |
|--------|---------|
| 焦虑、复习不完、来不及、担心 | 焦虑 |
| 累、疲惫、困、不想动 | 疲惫 |
| 开心、充实、完成、进步 | 开心 |
| 想家、孤独、一个人 | 难过 |
| 其他 | 平静 |

#### 2.3 DeepSeek API 集成

**端点**：`POST https://api.deepseek.com/v1/chat/completions`

**模型**：`deepseek-chat`

**API Key 管理**：通过 `--dart-define=DEEPSEEK_API_KEY=xxx` 编译参数传入，未设置时使用空字符串，API 调用直接降级。

**接口方法**：

| 方法 | 用途 | 参数 |
|------|------|------|
| `chatText()` | 单轮对话（动态 AI 首评） | userMessage, systemPrompt（可选） |
| `chatConversation()` | 多轮对话（AI 聊天 + 动态追问） | history（List\<Map\>） |
| `generateDailySummary()` | 每日小结 | completedCount, totalCount, focusMinutes, nickname |

---

### 模块三：全局支撑

#### 3.1 本地持久化

**存储策略**：

| 存储方式 | 存储内容 | Box / Key |
|---------|---------|-----------|
| Hive | 待办列表 | Box 'todos' |
| Hive | 目标列表（含子目标） | Box 'goals' |
| Hive | 用户信息 | Box 'user_profile' |
| Hive | 番茄钟记录 | Box 'pomodoro_records' |
| Hive | 生活动态 + AI 回复 | Box 'posts' |
| Hive | AI 对话记录 | Box 'chat_sessions' |
| SharedPreferences | 深色模式开关 | key 'theme_mode' |
| SharedPreferences | 番茄钟默认分钟数 | key 'pomodoro_default' |
| SharedPreferences | 每日小结通知开关 | key 'day_summary' |
| SharedPreferences | 待办提醒通知开关 | key 'todo_reminder' |
| SharedPreferences | 目标截止提醒通知开关 | key 'goal_reminder' |

**初始化流程**：
1. `WidgetsFlutterBinding.ensureInitialized()`
2. `Hive.initFlutter(appDir.path)`
3. 依次打开 6 个 Hive Box
4. 初始化通知插件
5. 初始化日期格式化 `initializeDateFormatting('zh_CN')`
6. 读取 SharedPreferences 主题设置 → 确定初始主题
7. `runApp(MultiProvider(...))`

#### 3.2 本地通知

**通知类型**（共 7 种）：

| ID | 通知名称 | 触发条件 | 通知开关 Key |
|----|---------|---------|-------------|
| 1001 | 待办提醒 | 每日定时触发，未完成待办数 > 0 | `todo_reminder` |
| 1002 | 子目标截止提醒 | 子目标 deadline 前 3 天 | `goal_reminder` |
| 1003 | 目标截止提醒 | 目标 deadline 前 3 天 | `goal_reminder` |
| 1004 | 每日小结 | 每日定时触发 | `day_summary` |
| 1005 | 连续专注鼓励 | 连续 3 天有番茄钟记录时 | `day_summary` |
| 1006 | AI 评论就绪 | AI 评论异步返回后 | 始终发送（不可关闭） |
| 1007 | 应用使用提醒 | 长时间未打开 App 时 | `day_summary` |

**权限要求**：Android `POST_NOTIFICATIONS`（API 33+）。

#### 3.3 深色模式

**实现方式**：`MaterialApp.themeMode` 绑定 `ThemeProvider.themeMode`。

**色值定义**：

| 元素 | 浅色模式 | 深色模式 |
|------|---------|---------|
| 主色调 | `#F98C53`（橙色） | `#F98C53`（不变） |
| 辅助色 | `#D2E0AA`（淡绿） | `#D2E0AA`（不变） |
| 页面背景 | `#F9F2EF`（浅米白） | `#1A1A1A` |
| 卡片背景 | `#FFFFFF` | `#2D2D2D` |
| 主要文字 | `#333333` | `#F0F0F0` |
| 辅助文字 | `#999999` | `#AAAAAA` |

**切换规则**：用户切换 Switch 后 `SharedPreferences.setBool('theme_mode', value)` 写入，App 重启时读取该值还原。

---

## 界面说明

### 导航架构

App 使用 `MainShell` 作为根容器，包含 `BottomNavigationBar`（4 Tab）和 `IndexedStack`（保持各 Tab 页面状态）。路由通过 `AppRouter.generateRoute` 集中管理。

**4 个底部 Tab**：

| 索引 | 标签 | 图标 | 页面 |
|------|------|------|------|
| 0 | 首页 | `home_rounded` | HomePage |
| 1 | 日历 | `calendar_month` | CalendarPage |
| 2 | 生活 | `favorite_rounded` | LifePage |
| 3 | 我的 | `person_rounded` | ProfilePage |

### 页面清单

| 页面 | 路由 | 参数 |
|------|------|------|
| MainShell（根容器） | `/` | — |
| PomodoroPage | `/pomodoro` | 可选 `int` 自定义分钟数 |
| TodoFormPage | `/todoForm` | 可选 `Map{'date': DateTime}` 指定日期 |
| TodoDetailPage | `/todoDetail` | 必传 `String` 待办 ID |
| GoalFormPage | `/goalForm` | — |
| GoalDetailPage | `/goalDetail` | 必传 `String` 目标 ID |
| PostFormPage | `/postForm` | — |
| PostDetailPage | `/postDetail` | 必传 `Map` 含动态 ID |
| ProfileEditPage | `/profileEdit` | — |
| NotificationSettingsPage | `/notificationSettings` | — |
| UsageStatsPage | `/usageStats` | — |

### 首页问候语规则

根据当前时间显示不同问候语：

| 时段 | 问候语 |
|------|--------|
| 05:00 ~ 11:59 | 早上好 |
| 12:00 ~ 13:59 | 中午好 |
| 14:00 ~ 17:59 | 下午好 |
| 18:00 ~ 23:59 | 晚上好 |
| 00:00 ~ 04:59 | 夜深了 |

格式：`☀️ {问候语}，{昵称}`

---

## 数据说明

### 数据流转规则

```
Page  →  Provider  →  Service/Hive  →  Storage
  ↓        ↓           ↓
Models   Models      Models
```

- Page 层只通过 `context.watch<T>()` / `context.read<T>()` 订阅 Provider。
- Provider 层持有内存数据，提供增删改接口，每次修改后调用 Hive 持久化 + `notifyListeners()`。
- Service 层封装 HTTP 请求（DeepSeek API）和通知发送。
- Model 层是纯 Dart 数据对象，包含 `toJson()` / `fromJson()` 序列化方法。

### Provider 数据流

| Provider | 数据 | 消费页面 |
|----------|------|---------|
| ThemeProvider | ThemeMode, isDarkMode | ProfilePage, 全局主题 |
| TodoProvider | List\<Todo\> | HomePage, CalendarPage, PomodoroPage, ProfilePage, UsageStatsPage |
| GoalProvider | List\<Goal\> | HomePage, CalendarPage, GoalDetailPage, GoalFormPage |
| UserProvider | UserProfile | HomePage, ProfilePage, ProfileEditPage |
| PomodoroProvider | List\<PomodoroRecord\> | CalendarPage, PomodoroPage, ProfilePage, UsageStatsPage |
| LifeProvider | List\<Post\>, ChatSession | LifePage, PostFormPage, PostDetailPage |

### 数据初始化降级策略

所有 Provider 初始化时先从 Hive 加载数据：
- Hive 中有数据 → 使用真实数据。
- Hive 中无数据（首次启动）→ 使用 Mock 数据（预置示例待办、目标、动态等），确保用户打开 App 即可看到完整 UI。

---

## 异常处理

### 网络异常（DeepSeek API）

| 异常类型 | 处理策略 |
|---------|---------|
| 网络不可达（SocketException） | 返回降级文案，不阻塞 UI 流程 |
| API 返回非 200 状态码 | 返回降级文案 |
| 请求超时（15 秒） | 返回降级文案 |
| 响应 JSON 解析失败 | 返回降级文案 |

**降级文案**：
- 动态 AI 首评：`"你今天的心情我收到啦，先抱抱你呀～"`
- AI 对话回复：`"我暂时无法回复呢，请稍后再试～"`
- 每日小结：`"今天辛苦了，好好休息，明天继续加油 ✨"`

**用户体验保障**：
- 发布动态时先写入 Hive 再异步请求 AI 评论，即使 API 失败动态也已保存。
- AI 对话中消息立即显示在 UI 上，失败只影响 AI 回复气泡。

### 通知异常

- 通知插件初始化失败 → 静默忽略，不影响主流程。
- 用户关闭某类通知 → 对应的 `_isEnabled()` 返回 false，跳过发送。
- 通知权限被系统拒绝 → 调用 `show()` 静默失败，不抛异常。

### 存储异常

- Hive Box 读取失败 → 降级使用空列表 / 默认值。
- SharedPreferences 读取失败 → 使用默认值（浅色模式、所有通知开关默认开启、番茄钟默认 25 分钟）。
- 图片选取后读取字节失败 → SnackBar 提示"图片读取失败，请重试"。

### 输入校验

| 校验项 | 规则 | 失败处理 |
|--------|------|---------|
| 待办标题 | 不能为空 | SnackBar "请输入待办标题" |
| 动态发布 | content 和 image 至少一个非空 | 发布按钮置灰禁用 |
| 心情选择 | 必须选择 | 默认不选中，发布按钮需内容或图片非空 |
| 番茄钟自定义时长 | 范围 1 ~ 120 分钟 | 输入超出范围时截断为边界值 |

---

## 验收标准

### 目标与考试规划

| 编号 | 验收项 | 验收标准 | 验证方式 |
|------|--------|---------|---------|
| G-01 | 创建长期目标 | 输入标题、描述、截止日期后目标出现在首页和日历页 | 手动操作验证 |
| G-02 | 目标状态切换 | 在目标详情页点击状态按钮可在"进行中"和"已完成"间切换 | 手动操作验证 |
| G-03 | 子目标管理 | 添加子目标后列表即时更新；勾选子目标完成后主目标 progress 自动重新计算 | 添加 3 个子目标,标记 1 个完成,验证 progress = 33% |
| G-04 | 目标删除 | 删除目标后首页和日历页同步移除 | 手动操作验证 |
| G-05 | 目标截止提醒 | 目标 deadline 前 3 天收到本地通知 | 修改系统日期或构造 deadline = 3 天后的目标验证 |

### 待办与日历

| 编号 | 验收项 | 验收标准 | 验证方式 |
|------|--------|---------|---------|
| T-01 | 新增待办 | 填写标题、选择日期、优先级、时间后，待办出现在日历对应日期 | 手动操作验证 |
| T-02 | 待办完成切换 | 首页或日历页点击待办可切换完成/未完成，另一页面同步更新 | 在首页标记完成，切换到日历页验证状态一致 |
| T-03 | 日历月视图 | 月视图显示当月所有日期，有待办的日期显示圆点，圆点颜色按当日最高优先级 | 创建不同优先级待办，观察圆点颜色 |
| T-04 | 日期筛选 | 点击日期展示当日待办列表、完成率、专注时长 | 选择有待办的日期验证数据正确 |
| T-05 | 月份导航 | 左右滑动切换月份（含跨年），月份标题同步更新 | 滑动验证 |
| T-06 | 空标题校验 | 标题为空时无法提交，SnackBar 提示 | 不输入标题点击提交 |

### 番茄钟

| 编号 | 验收项 | 验收标准 | 验证方式 |
|------|--------|---------|---------|
| P-01 | 默认计时 | 首页点击 [开始] 启动 25 分钟倒计时 | 观察倒计时起始值为 25:00 |
| P-02 | 自定义时长 | 首页点击 [自定义]，输入分钟数后启动 | 输入 30，验证倒计时起始值为 30:00 |
| P-03 | 自定义时长范围 | 输入 > 120 截断为 120，输入 < 1 截断为 1 | 输入 0 和 200 验证 |
| P-04 | 待办绑定 | 下拉菜单展示当日未完成待办列表 | 创建 3 个待办，验证下拉菜单显示 3 个 |
| P-05 | 绑定待办自动完成 | 绑定的番茄钟完成时，对应待办自动标记为完成 | 绑定待办 → 结束计时 → 验证待办状态变为已完成 |
| P-06 | 专注记录写入 | 计时完成或手动结束后，PomodoroRecord 写入 Hive | 结束计时 → 切换到"我的"页 → 专注次数 +1 |
| P-07 | 重置功能 | 计时中点击重置，计时器回到初始时长 | 运行 30 秒后点重置，验证恢复到初始时长 |
| P-08 | 暂停/继续 | 暂停时计时停止，继续后从暂停点恢复 | 暂停 10 秒后继续，验证倒计时值未跳变 |

### 生活动态

| 编号 | 验收项 | 验收标准 | 验证方式 |
|------|--------|---------|---------|
| L-01 | 发布纯文字动态 | 选择心情 + 输入文字 → 发布 → 动态卡片立即显示在时间线顶部 | 手动操作验证 |
| L-02 | 发布图文动态 | 选择心情 + 拍摄/选择照片 → 发布 → 动态卡片包含图片预览 | 手动操作验证 |
| L-03 | 空内容校验 | 文字为空且未选图时发布按钮置灰不可点击 | 清空内容和图片,验证按钮状态 |
| L-04 | 心情必选 | 心情未选择时不允许发布（除非选了图片） | 验证逻辑 |
| L-05 | AI 评论生成 | 发布后异步获取 AI 评论,评论返回后显示在动态卡片上 | 发布后等待数秒,观察 AI 评论出现 |
| L-06 | AI 评论降级 | 断网或 API 失败时显示降级文案,动态卡片不空白 | 关闭网络后发布动态验证 |
| L-07 | 动态删除 | 点击删除按钮 → 确认弹窗 → 确认后动态从列表移除 | 手动操作验证 |
| L-08 | 动态多轮追问 | 在动态详情页输入追问,AI 基于上下文回复 | 在动态详情页输入 3 轮对话验证 |
| L-09 | 图片裁剪 | 选择的图片自动裁剪为正方形缩略图 | 选择非正方形图片,验证预览为正方形 |

### AI 对话

| 编号 | 验收项 | 验收标准 | 验证方式 |
|------|--------|---------|---------|
| A-01 | 多轮对话 | 发送消息后收到 AI 回复,连续对话保持上下文 | 连续 5 轮对话验证上下文连贯 |
| A-02 | 对话持久化 | 退出生活页再进入,历史对话保留 | 发送 3 条消息 → 切换到其他 Tab → 切回,验证消息保留 |
| A-03 | API 降级 | 断网时发送消息,显示降级回复 | 关闭网络后发送消息验证 |
| A-04 | 回复长度 | AI 每条回复不超过 30 字 | 发送多条消息,统计 AI 回复字数 |
| A-05 | 对话截断 | 超过 40 条消息时仅取最近 40 条构建 context | 发送 25 轮,验证早期上下文可能丢失但近期保持 |

### 个人中心与设置

| 编号 | 验收项 | 验收标准 | 验证方式 |
|------|--------|---------|---------|
| U-01 | 个人信息编辑 | 修改昵称/账号/签名 → 点击保存 → 首页和我的页同步更新 | 修改昵称后验证首页问候语变化 |
| U-02 | 头像更换 | 选择新头像 → 保存 → 我的页头像更新 | 手动操作验证 |
| U-03 | 深色模式切换 | 切换 Switch → 全局主题实时变化 | 切换后检查所有页面 |
| U-04 | 深色模式持久化 | 切换深色模式 → 杀进程重启 → 保持深色模式 | 切换后重启 App 验证 |
| U-05 | 通知开关 | 关闭各类通知 → 不再收到对应通知 | 关闭待办提醒,创建 deadline 临近的目标验证不弹通知 |
| U-06 | 统计数据准确性 | 专注次数 = PomodoroRecord 总数,待办完成 = done=true 的 Todo 数 | 完成 3 个待办 + 2 个番茄钟,验证统计卡片数字 |
| U-07 | 使用统计页 | 展示本周完成率、今日专注时长、近 7 天柱状图 | 手动验证数据准确性 |

### 数据持久化

| 编号 | 验收项 | 验收标准 | 验证方式 |
|------|--------|---------|---------|
| D-01 | 全量数据持久化 | 创建数据后杀进程重启,所有数据（待办、目标、番茄钟记录、动态、AI 对话历史、用户信息）完整保留 | 每个类型创建至少 1 条数据 → 杀进程 → 重启验证 |
| D-02 | 动态数据持久化 | AI 评论异步返回后杀进程重启,评论保留 | 发布动态等待 AI 评论出现 → 重启验证 |
| D-03 | 设置持久化 | 主题、通知开关、番茄钟默认时长杀进程后保持 | 修改所有设置 → 重启验证 |

### 兼容性

| 编号 | 验收项 | 验收标准 | 验证方式 |
|------|--------|---------|---------|
| C-01 | Android 最低版本 | 在 Android API 21 设备上正常运行,功能完整 | 模拟器或真机测试 |
| C-02 | 深色模式全页面 | 16 种颜色定义覆盖所有页面,无未适配的硬编码颜色 | 切换深色模式后逐页检查 |
| C-03 | 横竖屏切换 | 所有页面在横屏/竖屏切换时 UI 不变形、不崩溃 | 旋转屏幕验证 |
| C-04 | flutter analyze 零错误 | `flutter analyze` 命令输出 0 errors, 0 warnings | 命令行执行验证 |
