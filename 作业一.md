# 作业一：页面导航结构设计与实现

## 一、为什么移动应用需要导航？

移动应用的屏幕空间有限，无法像桌面应用那样将所有功能平铺在一个界面上。导航系统是移动应用的"骨架"，它解决以下核心问题：

| 问题 | 说明 |
|------|------|
| **信息分层** | 不同功能模块需要独立页面承载，避免单页面信息过载 |
| **用户认知** | 清晰的页面层级关系帮助用户建立空间记忆，降低认知负担 |
| **操作流引导** | 导航引导用户按预期路径完成操作（如：首页 → 待办详情 → 编辑） |
| **状态保持** | 页面栈机制让用户在返回上一页时能恢复之前的状态 |

---

## 二、Flutter Navigator 与路由管理原理

### 2.1 Navigator —— 页面栈管理器

Flutter 使用 **Navigator** 组件管理页面栈（Route Stack），遵循"后进先出"（LIFO）原则：

```
                 ┌─────────────┐
    push()  →    │  Route C     │  ← 栈顶（当前可见）
                 ├─────────────┤
                 │  Route B     │
                 ├─────────────┤
                 │  Route A     │  ← 栈底（首页）
                 └─────────────┘

    pop()   ←    │  Route B     │  ← C被弹出，B变为栈顶
                 ├─────────────┤
                 │  Route A     │
                 └─────────────┘
```

### 2.2 两种路由管理方式

Flutter 提供两种路由注册方式：

| 方式 | 说明 | 适用场景 |
|------|------|----------|
| **静态路由表**（`routes`） | 在 MaterialApp 中预先注册所有路由名 → 页面映射 | 简单、无需传参的页面跳转 |
| **动态路由生成**（`onGenerateRoute`） | 通过回调函数按路由名动态创建 Route 对象 | 需要传参、复杂跳转逻辑 |

**本项目选择 `onGenerateRoute` 方式**，原因如下：

- 多个页面需要接收参数（如番茄钟时长、待办ID、目标ID等）
- 统一管理路由逻辑，便于维护
- 可处理未知路由的兜底页面

### 2.3 导航核心 API

```dart
// 跳转到新页面（入栈）
Navigator.pushNamed(context, '/todoDetail', arguments: todoId);

// 返回上一页（出栈）
Navigator.pop(context);

// 跳转到新页面并移除当前页（替换）
Navigator.pushReplacementNamed(context, '/home');

// 跳转到命名路由（静态方式）
Navigator.push(context, MaterialPageRoute(builder: (_) => TargetPage()));
```

### 2.4 页面间数据传递方式

本项目采用两种数据传递方式：

**方式一：路由参数传递（RouteSettings.arguments）**

适用于一次性传递的简单数据：

```dart
// 源页面：传递参数
Navigator.pushNamed(context, AppRoutes.pomodoro, arguments: 25);

// onGenerateRoute 中解包参数
case AppRoutes.pomodoro:
  final customMinutes = settings.arguments as int?;
  return MaterialPageRoute(
    builder: (_) => PomodoroPage(customMinutes: customMinutes),
  );
```

**方式二：Provider 全局状态共享**

适用于需要多个页面同步的数据（如待办列表、目标进度）：

```dart
final todoProvider = context.read<TodoProvider>();
todoProvider.toggleTodo(todo.id);  // 所有监听的页面同步更新
```

**选型原则**：

| 数据特征 | 使用方式 |
|----------|----------|
| 一次性使用、单向传递 | 路由参数（arguments） |
| 多页面共享、需同步更新 | Provider 全局状态 |

---

## 三、页面跳转关系图

### 3.1 页面清单（共13个页面）

| 页面名称 | 路由路径 | 页面类 | 说明 |
|----------|----------|--------|------|
| 主框架 | `/` | `MainShell` | 底部导航栏框架，含4个Tab页 |
| 首页 | `/`（Tab0） | `HomePage` | 今日待办、番茄钟、目标进度 |
| 日历 | `/`（Tab1） | `CalendarPage` | 日历视图 + 当日待办管理 |
| 生活 | `/`（Tab2） | `LifePage` | 生活动态列表 |
| 我的 | `/`（Tab3） | `ProfilePage` | 个人信息 + 设置 |
| 番茄钟 | `/pomodoro` | `PomodoroPage` | 专注计时器 |
| 待办表单 | `/todoForm` | `TodoFormPage` | 新增待办事项 |
| 待办详情 | `/todoDetail` | `TodoDetailPage` | 查看/编辑待办详情 |
| 目标表单 | `/goalForm` | `GoalFormPage` | 新增目标 |
| 目标详情 | `/goalDetail` | `GoalDetailPage` | 查看/编辑目标详情 |
| 动态表单 | `/postForm` | `PostFormPage` | 发布生活动态 |
| 动态详情 | `/postDetail` | `PostDetailPage` | 查看动态详情 |
| 个人信息编辑 | `/profileEdit` | `ProfileEditPage` | 编辑头像、昵称、签名等 |

### 3.2 页面跳转关系图

```
                              ┌─────────────────────────────────────┐
                              │           MaterialApp               │
                              │    onGenerateRoute = AppRouter      │
                              │                                     │
                              │  ┌─────────────────────────────┐    │
                              │  │         MainShell  (/)       │    │
                              │  │                             │    │
                              │  │  ┌──────────┐ ┌──────────┐  │    │
                              │  │  │ 首页     │ │ 日历     │  │    │
                              │  │  │ HomePage │ │Calendar  │  │    │
                              │  │  │ Tab 0    │ │Page      │  │    │
                              │  │  │          │ │ Tab 1    │  │    │
                              │  │  └────┬─────┘ └────┬─────┘  │    │
                              │  │       │            │        │    │
                              │  │  ┌──────────┐ ┌──────────┐  │    │
                              │  │  │ 生活     │ │ 我的     │  │    │
                              │  │  │ LifePage │ │Profile   │  │    │
                              │  │  │ Tab 2    │ │Page      │  │    │
                              │  │  │          │ │ Tab 3    │  │    │
                              │  │  └────┬─────┘ └────┬─────┘  │    │
                              │  │       │            │        │    │
                              │  └───────┼────────────┼────────┘    │
                              └──────────┼────────────┼─────────────┘
                                         │            │
         ┌───────────────────────────────┼────────────┼───────────────────────┐
         │                               │            │                       │
         ▼                               ▼            ▼                       ▼
   ┌───────────┐                  ┌───────────┐ ┌───────────┐          ┌───────────┐
   │  番茄钟    │                  │  目标表单  │ │  动态表单  │          │ 个人信息   │
   │ /pomodoro │                  │ /goalForm │ │ /postForm │          │ 编辑页    │
   │           │                  │           │ │           │          │/profile   │
   │ 参数:     │                  │ 新增目标   │ │ 发布动态   │          │  Edit     │
   │ customMinutes              │           │ │           │          │           │
   └───────────┘                  └─────┬─────┘ └─────┬─────┘          │ 编辑昵称   │
                                        │             │                │ 头像/签名  │
                                        │             │                └───────────┘
                                        ▼             ▼
                                  ┌───────────┐ ┌───────────┐
                                  │  目标详情  │ │  动态详情  │
                                  │/goalDetail│ │/postDetail│
                                  │           │ │           │
                                  │ 参数:     │ │ 参数:     │
                                  │ goalId    │ │ id+content│
                                  └───────────┘ └───────────┘


         ┌─────────────────────────────────────────────────────────────┐
         │              从日历页 (CalendarPage) 跳转                     │
         │                                                             │
         ▼                                                             ▼
   ┌───────────┐                                                ┌───────────┐
   │  待办表单  │                                                │  待办详情  │
   │ /todoForm │                                                │/todoDetail│
   │           │                                                │           │
   │ 参数:     │                                                │ 参数:     │
   │ date (选) │                                                │ todoId    │
   └───────────┘                                                └───────────┘
```

### 3.3 跳转关系明细表

| 序号 | 源页面 | 触发操作 | 目标页面 | 路由路径 | 传递参数 |
|------|--------|----------|----------|----------|----------|
| ① | 首页 — 待办卡片"开始专注" | 点击按钮 | 番茄钟 | `/pomodoro` | `customMinutes: int?` |
| ② | 首页 — "+"新增按钮 | 点击按钮 | 目标表单 | `/goalForm` | 无 |
| ③ | 首页 — 目标卡片 | 点击卡片 | 目标详情 | `/goalDetail` | `goalId: String` |
| ④ | 日历 — "新增"按钮 | 点击按钮 | 待办表单 | `/todoForm` | `{date: DateTime}` |
| ⑤ | 日历 — 待办项 | 点击待办 | 待办详情 | `/todoDetail` | `todoId: String` |
| ⑥ | 生活 — "+"发布按钮 | 点击按钮 | 动态表单 | `/postForm` | 无 |
| ⑦ | 生活 — 动态卡片 | 点击卡片 | 动态详情 | `/postDetail` | `{id, content}` |
| ⑧ | 我的 — 编辑按钮 | 点击按钮 | 个人信息编辑 | `/profileEdit` | 无（走Provider共享） |
| ⑨ | 所有子页面 — 返回按钮 | 点击/手势 | 上一页 | — | `Navigator.pop()` |

### 3.4 导航要点总结

| 特性 | 实现方式 |
|------|----------|
| Tab 切换 | `IndexedStack` + `NavigationBar`，不走 Navigator 栈，切换不丢状态 |
| 子页面跳转 | `Navigator.pushNamed(context, route, arguments: data)` 入栈 |
| 子页面返回 | `Navigator.pop(context)` 出栈，回到上一个页面 |
| 路由参数传递 | `RouteSettings.arguments` → `AppRouter.generateRoute` 解包 → 构造函数 |
| 共享数据同步 | 不通过路由传递，直接读 `Provider`（待办、目标、用户信息） |
| 兜底处理 | 未知路由显示 "未找到路由" 提示页，不崩溃 |

---

## 四、main.dart 中的 routes 定义

本项目采用 `onGenerateRoute` 动态路由方式，路由定义拆分为三个文件，形成完整的路由系统：

### 4.1 路由常量定义

代码位置：`flutter_harmonyos/lib/routes/app_routes.dart`

```dart
class AppRoutes {
  static const String pomodoro = '/pomodoro';
  static const String todoForm = '/todoForm';
  static const String todoDetail = '/todoDetail';
  static const String goalForm = '/goalForm';
  static const String goalDetail = '/goalDetail';
  static const String postForm = '/postForm';
  static const String postDetail = '/postDetail';
  static const String profileEdit = '/profileEdit';
}
```

### 4.2 动态路由生成器

代码位置：`flutter_harmonyos/lib/routes/app_router.dart`

```dart
import 'package:flutter/material.dart';
import 'package:flutter_harmonyos/routes/app_routes.dart';
import 'package:flutter_harmonyos/pages/calendar/todo_form_page.dart';
import 'package:flutter_harmonyos/pages/calendar/todo_detail_page.dart';
import 'package:flutter_harmonyos/pages/calendar/goal_form_page.dart';
import 'package:flutter_harmonyos/pages/calendar/goal_detail_page.dart';
import 'package:flutter_harmonyos/pages/life/post_form_page.dart';
import 'package:flutter_harmonyos/pages/life/post_detail_page.dart';
import 'package:flutter_harmonyos/pages/pomodoro/pomodoro_page.dart';
import 'package:flutter_harmonyos/pages/profile/profile_edit_page.dart';

class AppRouter {
  static Route<dynamic> generateRoute(RouteSettings settings) {
    switch (settings.name) {
      case AppRoutes.pomodoro:
        final customMinutes = settings.arguments as int?;
        return MaterialPageRoute(
          builder: (_) => PomodoroPage(customMinutes: customMinutes),
        );

      case AppRoutes.todoForm:
        final args = settings.arguments as Map<String, dynamic>?;
        return MaterialPageRoute(
          builder: (_) => TodoFormPage(initialDate: args?['date'] as DateTime?),
        );

      case AppRoutes.todoDetail:
        final todoId = settings.arguments as String?;
        return MaterialPageRoute(
          builder: (_) => TodoDetailPage(todoId: todoId ?? ''),
        );

      case AppRoutes.goalForm:
        return MaterialPageRoute(
          builder: (_) => const GoalFormPage(),
        );

      case AppRoutes.goalDetail:
        final goalId = settings.arguments as String?;
        return MaterialPageRoute(
          builder: (_) => GoalDetailPage(goalId: goalId ?? ''),
        );

      case AppRoutes.postForm:
        return MaterialPageRoute(
          builder: (_) => const PostFormPage(),
        );

      case AppRoutes.postDetail:
        final args = settings.arguments as Map<String, dynamic>?;
        return MaterialPageRoute(
          builder: (_) => PostDetailPage(
            postId: args?['id'] as String? ?? '',
            content: args?['content'] as String? ?? '',
          ),
        );

      case AppRoutes.profileEdit:
        return MaterialPageRoute(
          builder: (_) => const ProfileEditPage(),
        );

      default:
        return MaterialPageRoute(
          builder: (_) => Scaffold(
            body: Center(child: Text('未找到路由: ${settings.name}')),
          ),
        );
    }
  }
}
```

### 4.3 MaterialApp 中注册

代码位置：`flutter_harmonyos/lib/main.dart`（关键片段）

```dart
void main() async {
  WidgetsFlutterBinding.ensureInitialized();
  await initializeDateFormatting('zh_CN');
  runApp(
    MultiProvider(
      providers: [
        ChangeNotifierProvider(create: (_) => ThemeProvider()),
        ChangeNotifierProvider(create: (_) => TodoProvider()),
        ChangeNotifierProvider(create: (_) => GoalProvider()),
        ChangeNotifierProvider(create: (_) => UserProvider()),
      ],
      child: const MyApp(),
    ),
  );
}

class MyApp extends StatelessWidget {
  const MyApp({super.key});

  @override
  Widget build(BuildContext context) {
    final themeProvider = context.watch<ThemeProvider>();

    return MaterialApp(
      title: '智能日常助手',
      debugShowCheckedModeBanner: false,
      themeMode: themeProvider.themeMode,
      theme: _buildLightTheme(),
      darkTheme: _buildDarkTheme(),
      initialRoute: '/',
      onGenerateRoute: AppRouter.generateRoute,  // ← 核心：注册动态路由生成器
      home: const MainShell(),                     // ← 默认首页（底部导航框架）
    );
  }
}
```

> **关键设计说明**：
> - `home` 和 `onGenerateRoute` 同时存在时，`home` 对应 `/` 路由
> - `MainShell` 内部使用 `IndexedStack` + `NavigationBar` 实现底部4个Tab的切换
> - Tab之间的切换不走 Navigator 栈，而是在同一个页面内通过 `setState` 切换，保证切换不丢失状态
> - 共定义 **8 条命名路由**，覆盖 13 个页面，均支持路由参数传递和未知路由兜底处理

---

## 五、用 ProcessOn 画页面跳转关系图 —— 逐步操作指南

### 5.1 整体画图架构（先理清层次再动手）

画图前需要理清的树形层级结构：

```
MaterialApp (onGenerateRoute)          ← 顶层容器
  └─ MainShell (/)                     ← 底部导航框架（4个Tab）
       ├─ 首页 HomePage (Tab 0)        ← Tab页面
       │    ├─ 番茄钟 /pomodoro        ← 子页面
       │    ├─ 目标表单 /goalForm       ← 子页面
       │    └─ 目标详情 /goalDetail    ← 子页面
       ├─ 日历 CalendarPage (Tab 1)
       │    ├─ 待办表单 /todoForm
       │    └─ 待办详情 /todoDetail
       ├─ 生活 LifePage (Tab 2)
       │    ├─ 动态表单 /postForm
       │    └─ 动态详情 /postDetail
       └─ 我的 ProfilePage (Tab 3)
            └─ 个人信息编辑 /profileEdit
```

**画图原则**：从上到下分层 → 同层元素水平对齐 → 父子关系用箭头连接 → 线条上加文字标注。

### 5.2 第一步：进入 ProcessOn 并新建流程图

1. 打开浏览器访问 **https://www.processon.com**
2. 注册 / 登录账号
3. 点击左上角 **"新建"** → 选择 **"流程图"**（Flowchart）

### 5.3 第二步：创建第一层 —— MaterialApp（顶层容器）

| 操作 | 说明 |
|------|------|
| 拖入控件 | 从左侧图形库拖入 **"圆角矩形"** 到画布中央靠上位置 |
| 设置尺寸 | 宽度 600px，高度 500px（需要留空间放内部元素） |
| 输入文字 | 双击输入 `MaterialApp (onGenerateRoute)` |
| 设置样式 | 填充色：浅蓝色 `#E3F2FD`，边框色 `#1565C0`，边框宽度 2px |

**作用**：这个矩形代表整个 App 的路由入口，所有页面都在它内部。

### 5.4 第三步：创建第二层 —— MainShell（底部导航框架）

| 操作 | 说明 |
|------|------|
| 拖入控件 | 在 MaterialApp 矩形**内部**拖入一个 **"矩形"** |
| 设置尺寸 | 宽度 520px，高度 300px |
| 输入文字 | 双击输入 `MainShell (/) — 底部导航` |
| 设置样式 | 填充色：浅橙色 `#FFF5F0`，边框色 `#F98C53`，边框宽度 2px，圆角 12px |

**作用**：表示底部导航框架，内部包含4个 Tab 页。

### 5.5 第四步：创建第三层 —— 4个Tab页面

在 MainShell 内部水平放置 4 个小矩形：

| Tab | 文字内容 | 相对位置 |
|-----|---------|---------|
| Tab 0 | `首页 HomePage` | 最左边 |
| Tab 1 | `日历 CalendarPage` | 左边第二个 |
| Tab 2 | `生活 LifePage` | 右边第二个 |
| Tab 3 | `我的 ProfilePage` | 最右边 |

**统一样式设置**：

| 属性 | 值 |
|------|-----|
| 尺寸 | 宽 110px，高 50px |
| 填充色 | 白色 `#FFFFFF` |
| 边框色 | `#F98C53` |
| 边框宽度 | 1.5px |
| 圆角 | 8px |
| 字体 | 微软雅黑，12px，加粗 |

**操作技巧**：先做好一个，然后 Ctrl+C / Ctrl+V 复制出另外三个，拖到水平对齐位置。

### 5.6 第五步：创建第四层 —— 各Tab的子页面

在每个 Tab 页**下方**放置它对应的子页面，用更小的矩形：

**首页(HomePage)下方**（从首页向下拉出三个子页面）：

| 子页面 | 矩形文字 | 传递参数 |
|--------|---------|---------|
| 番茄钟 | `/pomodoro` | `customMinutes` |
| 目标表单 | `/goalForm` | 无 |
| 目标详情 | `/goalDetail` | `goalId` |

**日历(CalendarPage)下方**（从日历向下拉出两个子页面）：

| 子页面 | 矩形文字 | 传递参数 |
|--------|---------|---------|
| 待办表单 | `/todoForm` | `{date}` |
| 待办详情 | `/todoDetail` | `todoId` |

**生活(LifePage)下方**（从生活向下拉出两个子页面）：

| 子页面 | 矩形文字 | 传递参数 |
|--------|---------|---------|
| 动态表单 | `/postForm` | 无 |
| 动态详情 | `/postDetail` | `{id, content}` |

**我的(ProfilePage)下方**（从我的向下拉出一个子页面）：

| 子页面 | 矩形文字 | 传递参数 |
|--------|---------|---------|
| 个人信息编辑 | `/profileEdit` | 无（走Provider） |

**子页面统一样式**：

| 属性 | 值 |
|------|-----|
| 尺寸 | 宽 100px，高 40px |
| 填充色 | 白色 `#FFFFFF` |
| 边框色 | `#D2E0AA`（绿色边框，区分主Tab） |
| 边框宽度 | 1px |
| 圆角 | 6px |
| 字体 | 微软雅黑，11px |

### 5.7 第六步：画连接箭头

这是最关键的一步，箭头代表了页面之间的跳转关系。

**添加箭头的方法**：

| 操作 | 步骤 |
|------|------|
| 触发锚点 | 鼠标悬停在**源矩形边缘**，会浮现蓝色圆点 |
| 拖出连线 | 按住蓝色圆点拖动到**目标矩形边缘** |
| 调整线型 | 选中线条 → 右侧属性面板 → 线型选 `折线` |
| 添加箭头 | 属性面板勾选 `终点箭头`，箭头样式选实心三角 |
| 添加标注 | 双击线条中间 → 输入跳转说明文字 |

**每根线条的标注文字**：

| 起点 | 终点 | 线上标注文字 |
|------|------|-------------|
| 首页 | 番茄钟 | `点击"开始专注"按钮` |
| 首页 | 目标表单 | `点击"+"新增按钮` |
| 首页 | 目标详情 | `点击目标卡片` |
| 日历 | 待办表单 | `点击"新增"按钮` |
| 日历 | 待办详情 | `点击待办项` |
| 生活 | 动态表单 | `点击"+"发布按钮` |
| 生活 | 动态详情 | `点击动态卡片` |
| 我的 | 个人信息编辑 | `点击编辑按钮` |

**线条样式统一**：
- 线条颜色：`#F98C53`
- 线条粗细：1.5px
- 标注字体大小：10px

### 5.8 第七步：添加图例（加分项）

在画布右下角空白区域添加图例：

| 操作 | 说明 |
|------|------|
| 拖入大矩形 | 作为图例背景，填充浅灰 `#FAFAFA` |
| 内置小色块 + 文字 | 用小矩形展示每种颜色含义 |

```
┌─────────────────────────┐
│ 图例                     │
│ ┌────┐  主容器           │
│ │蓝色│  MaterialApp      │
│ └────┘                  │
│ ┌────┐  Tab页面          │
│ │橙色│  导航页面         │
│ └────┘                  │
│ ┌────┐  子页面           │
│ │绿色│  详情/表单页      │
│ └────┘                  │
│ ───→   页面跳转方向      │
│ 文字   跳转触发方式      │
└─────────────────────────┘
```

### 5.9 第八步：整体排版检查清单

画完后逐项检查：

- [ ] MaterialApp 在最外层，包裹所有内容
- [ ] MainShell 在 MaterialApp 内部
- [ ] 4个Tab页面在 MainShell 内部水平对齐
- [ ] 每个Tab的子页面在其下方，垂直对齐
- [ ] 所有箭头都从源页面指向目标页面
- [ ] 每根箭头线上都有触发方式标注
- [ ] 颜色区分明确（蓝=容器 / 橙=Tab页面 / 绿=子页面）
- [ ] 图例说明完整

### 5.10 第九步：导出图片

| 操作 | 步骤 |
|------|------|
| 导出 | 菜单栏 → 文件 → 导出 → **PNG 图片** |
| 分辨率 | 选 2x 或 3x（保证清晰） |
| 保存 | 保存到本地，插入作业文档中 |
| 备用 | 也可以截图（但PNG导出更清晰） |

---

## 六、自查清单

- [x] 理解并说明移动应用为什么需要导航
- [x] 掌握 Flutter Navigator 与路由管理（页面栈、push/pop）
- [x] 实现页面间数据传递（路由参数 + Provider 两种方式）
- [x] 理解 Hero 动画原理并规划应用场景
- [x] 画出完整的页面跳转关系图（ASCII图见上文，ProcessOn图待完成）
- [x] 在 main.dart 中配置 onGenerateRoute，定义所有路由
- [x] 实现至少8个页面的跳转与返回（超过最低要求的3个）
